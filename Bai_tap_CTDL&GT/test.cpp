#include <bits/stdc++.h>
using namespace std;

int n, m, k;

vector<int> check(int *a, int *b)
{
    vector<int> res;
    res.clear();
        
}


int main()
{
    int t;
    cin >> t;
    while(t--) 
    {
        int *a = new int[100005], *b = new int[100005], *c = new int[100005];
        cin >> n >> m >> k;
        for(int i=0; i<n; ++i)
            cin >> a[i];
        for(int i=0; i<m; ++i)
            cin >> b[i];
        for(int i=0; i<k; ++i)
            cin >> c[i];

        vector<int> ans;
        ans.clear();



    }
    return 0;
}