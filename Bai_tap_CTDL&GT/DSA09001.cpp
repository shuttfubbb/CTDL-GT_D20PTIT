#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n, k;
        vector<vector<int> > dothi;
        cin >> n >> k;
        for(int i=0; i<n; ++i)
        {
            vector<int> a;
            dothi.push_back(a);
        }
        while(k--)
        {
            
        }
    }
    return 0;
}